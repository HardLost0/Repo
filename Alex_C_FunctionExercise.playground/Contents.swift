class buyingCar {
    var engine = "Engine"
    var Color = "Color"
    var Model = "Model"
    var Year = "Year"
}
let car = buyingCar()
print("The possible feature you could select when buying a car is a \(car.engine),\(car.Color),\(car.Model),\(car.Year)")
